{
    "pyramidio": {
        "type": "foo",
        "foo.zookeeper.quorum": "${foo.zookeeper.quorum}",
        "foo.zookeeper.port": "${foo.zookeeper.port}",
        "foo.master": "${foo.master}"
    },
    "public": {
        "drilldown": {
            "endpoint": "${drilldown.endpoint}",
            "mode": "some_mode.txt"
        }
    }

}